import React from 'react'
import { Grid} from '@material-ui/core';

const usestyle = {
    containerThirdGrid: {
        backgroundColor: "#00b100", 
        height: "60px",
    }
}

export default function SendMessage () {
    return (
        <Grid item s={12} style={usestyle.containerThirdGrid} container spacing={0} direction="column" alignItems="center" justify="center">
            ldskljl
        </Grid>
    )
}